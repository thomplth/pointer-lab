# Gradle
.gradle/
build/
**/build/

# Kotlin
.kotlin/

# Android Studio / IntelliJ
.idea/
*.iml

# Local machine config (SDK path is machine-specific — never commit)
local.properties

# macOS
.DS_Store

# Keep the Gradle wrapper jar so the repo builds without a local Gradle install:
!gradle/wrapper/gradle-wrapper.jar
